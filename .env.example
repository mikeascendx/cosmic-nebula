# Cosmic Nebula is a fully client-side experience and needs no database to run.
# This var only exists for the bundled Prisma scaffold (src/lib/db.ts), which the
# app itself does not import. Safe to leave as-is.
DATABASE_URL=file:./db/custom.db
